var fs = require("fs");
fs.readFile("sample.txt", "utf8", function (err, data) {
  console.log(data);
});
// dir // input this word to see what files are in that current directory location. (from parent to child) that's it!
